package:
  name: yoga-test-docker
  description: Docker Compose management for integration tests
  dependencies:
    - aff: ">=7.0.0 <8.0.0"
    - effect: ">=4.0.0 <5.0.0"
    - exceptions: ">=6.0.0 <7.0.0"
    - prelude: ">=6.0.0 <7.0.0"
  build:
    strict: true
  publish:
    version: 0.1.0
    license: MIT
